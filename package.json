{
  "name": "gitx",
  "version": "1.0.0",
  "description": "Extended Git Operations CLI",
  "type": "module",
  "bin": {
    "gitx": "./dist/index.js"
  },
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "npm": "please-use-pnpm",
    "yarn": "please-use-pnpm"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "tsx src/index.ts",
    "build": "pnpm quality && tsup src/index.ts --format esm --clean --dts",
    "test": "vitest run --passWithNoTests",
    "lint": "biome check .",
    "lint:fix": "biome check --fix .",
    "reqcheck": "gherkin-lint requirements/",
    "typecheck": "tsc --noEmit",
    "quality": "./scripts/quality-dev.sh",
    "release": "changeset publish",
    "build-ci": "./scripts/quality-build.sh && tsup src/index.ts --format esm --clean --dts"
  },
  "dependencies": {
    "@clack/prompts": "catalog:",
    "citty": "catalog:",
    "execa": "catalog:",
    "picocolors": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@changesets/changelog-github": "catalog:",
    "@changesets/cli": "catalog:",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "gherkin-lint": "catalog:",
    "tsup": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "vitest-mock-extended": "catalog:"
  }
}
